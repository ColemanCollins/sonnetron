var expressConfig = {};
expressConfig.routes = {};
 
expressConfig.routes.raw = '/raw';
expressConfig.routes.feed = '/tweets';
expressConfig.routes.count = '/count';
 
module.exports = expressConfig;
for (var i = Things.length - 1; i >= 0; i--) {
  Things[i]
};